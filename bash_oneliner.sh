# Open the file "BIKES" through a Unix terminal, and run the command below
find * | grep json > files_list.txt

# This will construct a text file named "files_text.txt", 
# which will include all the json files with the motorcycle listings